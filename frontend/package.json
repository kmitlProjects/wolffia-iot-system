{
  "name": "wolffia-dashboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/assets && tsc --project tsconfig.json && cp src/index.html dist/index.html && cp src/styles.css dist/assets/styles.css && cp -R src/icon dist/assets/icon"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
